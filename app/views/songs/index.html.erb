<h1>All Songs</h1>

<% @songs.each do |song| %>
  <div id="song_<%= song.id %>">
    <p>Title: <%= song.title %></p>
    <p>Play Count: <%= song.play_count %></p>
    <p>Length: <%= song.length %></p>
  </div>
<% end %>
